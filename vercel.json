{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "pnpm install --no-frozen-lockfile",
  "buildCommand": "pnpm run build",
  "functions": {
    "api/score.py": { "memory": 256, "maxDuration": 10 },
    "api/health.go": { "memory": 128, "maxDuration": 10 }
  }
}
